export enum Gender {
  Nam,
  Nữ,
  Khác,
}

export enum Roles {
  CUSTOMER = "CUSTOMER",
  DRIVER = "DRIVER",
  BUSCOMPANY = "BUSCOMPANY",
  ADMIN = "ADMIN",
}

export enum CreateRoles {
  CUSTOMER,
  DRIVER,
  BUSCOMPANY,
  ADMIN
}
